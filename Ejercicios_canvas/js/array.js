let numero = prompt("Ingrese el multiplo");
let tamaño = prompt("Ingrese el tamaño del array");


function Arrays(numero, tamaño){
    let vector = [];
    for (let index = 1; index <= tamaño.length; index++) {
          
        vector.push(numero*i);
    }

    return vector;

}

console.log(Arrays());